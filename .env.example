# NEAR private key (ed25519:xxx format)
# Can also be set via:
#   near-intents config generate-key (generate new key)
#   near-intents config set private-key <key> (use existing key)
NEAR_PRIVATE_KEY=

# API key for fee-free swaps (optional)
# Without API key, swaps incur 0.1% fee
# Get free key: https://partners.near-intents.org/
# Can also be set via: near-intents config set api-key <key>
DEFUSE_JWT_TOKEN=
